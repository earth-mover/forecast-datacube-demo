channels:
    - conda-forge

dependencies:
    - arraylake >=0.13.1
    - cfgrib
    - dask
    - fsspec
    - herbie-data
    - s3fs
    - pydantic >2
    - xarray
    - pip:
      - modal
      - icechunk
